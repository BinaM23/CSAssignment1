
public class test1{
   public static void main(String[]args){
      System.out.println("debugga");
      for(int i=0;i<5;i++){
         if(i%2==0)
            System.out.println("even");
         else
            System.out.println("odd");
            
             Integer[] pixels=new Integer[the_pixels.size()];
                   pixels=the_pixels.toArray(pixels);
                   
                     //int sum=0;
                  /* for(int i=0;i<pixels.length;i++){
                    r += (float)((pixels[i] & 0x00ff0000) >> 16)/(window*window);
                    g += (float)((pixels[i] & 0x0000ff00) >>  8)/(window*window);
                    b += (float)((pixels[i] & 0x000000ff) >>  0)/(window*window);
                    
                    }
                    dpixel = (0xff000000) |((int)r << 16) |((int)g << 8) |((int)b << 0);*/

      
      }


   }
}